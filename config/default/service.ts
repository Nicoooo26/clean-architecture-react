const config = {
    // Credenciales del aplicativo
    authenticate: {
        accountname: 'Plantilla',
        //credentials: process.env.NODE_LOGIN_SERVER_CREDENTIALS, //|_FILE
    },
    // Encriptado de secretos
    encrypted_authenticate: {
        text: '',
    },
};

export default config;
